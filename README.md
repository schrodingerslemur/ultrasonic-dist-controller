# Ultrasonic Distance Controller (PID + UART Dashboard)

This project implements:
- Ultrasonic HC‑SR04 distance sensing  
- PID loop maintaining target distance  
- Motor control output (PWM‑style logic, generic MCU)  
- UART serial dashboard for live telemetry  

Build with:
```
make
```
